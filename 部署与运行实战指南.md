# 世界历史学习平台 - 部署与运行实战指南

> 本文档记录在 **Windows 环境**下从零开始部署并运行前后端项目的完整实战过程，包含实际遇到的问题、原因分析和解决方案。适用于与本项目环境类似的 Windows 用户。
>
> 理论部署请参考「前后端集成与部署指南.md」，本文档侧重实战操作和问题排查。

---

## 目录

1. [项目结构](#一项目结构)
2. [环境检测与配置](#二环境检测与配置)
3. [后端部署实战](#三后端部署实战)
4. [前端部署实战](#四前端部署实战)
5. [一键启动脚本使用](#五一键启动脚本使用)
6. [手机端访问配置](#六手机端访问配置)
7. [常见问题实战解决方案](#七常见问题实战解决方案)
8. [验证清单](#八验证清单)
9. [日常运维](#九日常运维)

---

## 一、项目结构

```
world-history-app/
├── 项目文档.html                    # 项目完整文档
├── 前后端集成与部署指南.md           # 通用部署指南（理论版）
├── 部署与运行实战指南.md             # 本文档（实战验证版）
├── API接口文档.md                    # 后端 API 接口文档
├── 更新日志.md                       # 版本迭代记录
├── 启动服务.bat                      # 一键启动脚本（双击即可运行）
├── 停止服务.bat                      # 停止服务脚本
├── start.ps1                         # PowerShell 启动逻辑
├── backend/                          # Express + SQLite 后端
│   ├── server.js                     # 服务入口（含前端静态托管+SPA回退+CSP配置）
│   ├── package.json                  # 依赖配置（better-sqlite3 ^11.7.0）
│   ├── config/database.js            # 数据库配置与建表（16张表）
│   ├── middleware/auth.js            # JWT 认证中间件
│   ├── routes/                       # 13个路由模块
│   │   ├── auth.js                   # 用户认证
│   │   ├── events.js                 # 历史事件
│   │   ├── people.js                 # 历史人物+关系图谱
│   │   ├── progress.js               # 学习进度
│   │   ├── notes.js                  # 学习笔记
│   │   ├── favorites.js              # 收藏
│   │   ├── quizzes.js                # 测验
│   │   ├── achievements.js           # 成就
│   │   ├── studySessions.js          # 学习时长
│   │   ├── stats.js                  # 统计
│   │   ├── sync.js                   # 数据同步
│   │   ├── data.js                   # 数据清除
│   │   └── health.js                 # 健康检查
│   ├── utils/                        # 工具脚本
│   │   ├── initDatabase.js           # 数据库初始化
│   │   ├── importData.js             # 数据导入
│   │   ├── importAllEvents.js        # 批量导入事件
│   │   ├── importPeopleRelations.js  # 导入人物关系
│   │   └── generateFrontendEvents.js # 生成前端events.ts
│   ├── data/                         # 历史数据 + SQLite 数据库
│   │   ├── china/                    # 中国史事件（19个文件）
│   │   ├── world/                    # 世界史事件（7个文件）
│   │   ├── people/                   # 人物关系（1个文件，44人33关系）
│   │   └── history.db                # SQLite 数据库（运行后生成）
│   └── README.md                     # 后端说明
└── front/                            # React 19 + Vite 7 前端
    ├── client/                       # 前端源码
    │   └── src/
    │       ├── pages/                # 18个页面组件
    │       ├── components/           # 通用组件
    │       ├── data/                 # 内置历史数据
    │       ├── store/                # Zustand 状态管理（useAppStore/useLearningStore）
    │       ├── utils/                # 工具函数（api.ts/safeStorage.ts）
    │       └── index.css             # 全局样式
    ├── dist/client/                  # 前端构建产物
    ├── package.json                  # 依赖配置（要求 Node >=22）
    └── vite.config.ts                # Vite 构建配置
```

---

## 二、环境检测与配置

### 2.1 系统要求

| 项目 | 最低要求 | 推荐配置 |
|------|----------|----------|
| 操作系统 | Windows 10 x64 | Windows 10/11 x64 |
| Node.js | 后端 >=16，前端 **>=22** | Node.js v22.x |
| npm | >=8.0 | >=10.0 |
| 内存 | 4GB | 8GB（前端构建需约 2GB） |
| 磁盘空间 | 2GB | 5GB |

### 2.2 检测当前环境

打开 PowerShell，依次执行：

```powershell
# 检测 Node.js 版本
node --version

# 检测 npm 版本
npm --version

# 检测系统架构
echo $env:PROCESSOR_ARCHITECTURE
```

**判断标准**：
- 如果 `node --version` 显示 v22.x.x → 环境满足要求，直接进入下一步
- 如果显示 v20.x.x 或更低 → 需要安装/使用便携版 Node v22

### 2.3 安装便携版 Node.js v22（推荐）

如果系统 Node 版本低于 22，**不要卸载系统 Node**，使用便携版即可：

1. **下载**：访问 [Node.js 官网](https://nodejs.org/)，下载 `Windows Binary (.zip)` 64位版本（如 `node-v22.12.0-win-x64.zip`）
2. **解压**：解压到项目同级目录，如 `E:\ProgramData\historyApp\node-v22\node-v22.12.0-win-x64\`
3. **验证**：
   ```powershell
   $nodePath = "E:\ProgramData\historyApp\node-v22\node-v22.12.0-win-x64"
   & "$nodePath\node.exe" --version
   # 应显示 v22.12.0
   ```

> ⚠️ **重要**：不要将便携版 Node 路径添加到系统环境变量，避免与系统 Node 冲突。每次使用前临时设置 PATH 即可。

### 2.4 设置临时环境变量（每次使用前执行）

```powershell
$nodePath = "E:\ProgramData\historyApp\node-v22\node-v22.12.0-win-x64"
$env:PATH = "$nodePath;$env:PATH"

# 验证
node --version  # 应显示 v22.x.x
npm --version   # 应显示 10.x.x
```

> 💡 **提示**：后续所有涉及 Node 的命令，都需要先执行上述临时环境变量设置。

---

## 三、后端部署实战

### 3.1 安装后端依赖

```powershell
# 1. 设置临时环境变量（如果还没设置）
$nodePath = "E:\ProgramData\historyApp\node-v22\node-v22.12.0-win-x64"
$env:PATH = "$nodePath;$env:PATH"

# 2. 进入后端目录
cd E:\ProgramData\historyApp\world-history-app\backend

# 3. 安装依赖
npm install
```

**预期输出**：
```
added 80 packages in 30s
```

> ⚠️ **常见问题**：如果 `better-sqlite3` 安装失败，检查 package.json 中版本是否为 ^11.7.0 及以上。旧版本（如 9.x）在 Node v22+ Windows 无预编译二进制。

### 3.2 初始化数据库

```powershell
node utils/initDatabase.js
```

**预期输出**：
```
数据库初始化完成
数据表创建成功
默认管理员创建成功
  - 邮箱: admin@history.com
  - 密码: admin123456
```

**验证**：检查 `backend/data/history.db` 文件是否已生成。

### 3.3 导入历史数据

```powershell
# 导入历史事件
node utils/importAllEvents.js

# 导入人物和关系
node utils/importPeopleRelations.js
```

**预期输出**：
```
===== 开始导入事件数据 =====
  共导入 37 个事件
===== 导入完成 =====

===== 开始导入人物和关系数据 =====
  人物 44, 关系 33
===== 导入完成 =====
```

### 3.4 启动后端服务

```powershell
node server.js
```

**预期输出**：
```
服务器运行在 http://localhost:3000
数据库连接成功
```

### 3.5 验证后端服务

**保持后端运行窗口打开**，另开一个 PowerShell 窗口：

```powershell
# 健康检查
Invoke-RestMethod -Uri "http://localhost:3000/api/health"

# 预期输出
# success   message       timestamp
# -------   -------       ---------
# True      API is running 2026-09-06T...
```

```powershell
# 测试登录
$body = @{ email = "admin@history.com"; password = "admin123456" } | ConvertTo-Json
Invoke-RestMethod -Uri "http://localhost:3000/api/auth/login" -Method Post -Body $body -ContentType "application/json"

# 预期输出：success=True，包含 user 和 token
```

---

## 四、前端部署实战

### 4.1 安装前端依赖

```powershell
# 1. 设置临时环境变量
$nodePath = "E:\ProgramData\historyApp\node-v22\node-v22.12.0-win-x64"
$env:PATH = "$nodePath;$env:PATH"

# 2. 进入前端目录
cd E:\ProgramData\historyApp\world-history-app\front

# 3. 安装依赖（必须加 --ignore-scripts）
npm install --ignore-scripts
```

> ⚠️ **必须使用 `--ignore-scripts`**：部分依赖的 postinstall 脚本在 Windows 可能失败，使用此参数跳过脚本执行。

**预期输出**：
```
added 500 packages in 2m
```

### 4.2 构建前端（Windows 正确方式）

```powershell
# 1. 确保在 front 目录
cd E:\ProgramData\historyApp\world-history-app\front

# 2. 设置生产环境变量
$env:NODE_ENV = "production"

# 3. 执行构建（不能用 npm run build:client！）
node node_modules/vite/bin/vite.js build --config vite.config.ts
```

> ⚠️ **重要警告**：
> - ❌ 不能使用 `npm run build:client`（Unix 语法在 Windows PowerShell 报错）
> - ✅ 必须使用上述命令手动设置环境变量后构建

**预期输出**：
```
vite v7.x.x building for production...
✓ 1200 modules transformed.
dist/client/index.html                   1.50 kB
dist/client/assets/index-BfmIUzhU.js  4,400.61 kB │ gzip: 1,390.46 kB
...
✓ built in 38.30s
```

### 4.3 替换 index.html 模板变量（关键步骤！）

构建后的 `index.html` 包含 11 个平台模板变量，**必须手动替换**，否则页面无法正常渲染。

#### 方法一：使用 PowerShell 脚本自动替换

```powershell
$indexPath = "E:\ProgramData\historyApp\world-history-app\front\dist\client\index.html"
$content = Get-Content $indexPath -Raw

# 替换模板变量
$content = $content -replace '\{\{csrfToken\}\}', '""'
$content = $content -replace '\{\{userId\}\}', '""'
$content = $content -replace '\{\{tenantId\}\}', '""'
$content = $content -replace '\{\{appId\}\}', '"app_17df6r2kvgn"'
$content = $content -replace '\{\{environment\}\}', '"production"'
$content = $content -replace '\{\{appName\}\}', '"世界历史学习平台"'
$content = $content -replace '\{\{\{appAvatar\}\}\}', '""'
$content = $content -replace '\{\{appDescription\}\}', '"世界历史学习平台"'
$content = $content -replace '\{\{basename\}\}', '"/"'
$content = $content -replace "JSON\.parse\('\{\{\{__platform__\}\}\}'\)", 'JSON.parse(''"web"'')'

# 替换 title 和 meta
$content = $content -replace '<title>\{\{appName\}\}</title>', '<title>世界历史学习平台</title>'
$content = $content -replace 'content="\{\{appName\}\}"', 'content="世界历史学习平台"'
$content = $content -replace 'content="\{\{appDescription\}\}"', 'content="世界历史学习平台"'
$content = $content -replace 'content="\{\{appAvatar\}\}"', 'content=""'

# 保存
Set-Content $indexPath -Value $content -Encoding UTF8 -NoNewline

# 验证：检查是否还有未替换的模板变量
$remaining = ([regex]::Matches($content, '\{\{[^}]+\}\}')).Count
Write-Host "剩余模板变量: $remaining"
# 应显示 0
```

#### 方法二：手动替换

用文本编辑器打开 `front/dist/client/index.html`，搜索 `{{`，逐个替换为上表中的值。

> ⚠️ **注意三花括号变量**：`{{{appAvatar}}}` 和 `{{{__platform__}}}` 使用三花括号，表示不转义的原始值。

### 4.4 主 JS 动态加载（移动端白屏修复）

为解决移动端 CSP 导致的白屏问题，需要将主 JS 改为动态加载。

在 `index.html` 中找到：
```html
<script type="module" crossorigin src="/assets/index-xxxx.js"></script>
```

替换为：
```html
<script>
(function(){
  var s=document.createElement('script');
  s.type='module';
  s.src='/assets/index-xxxx.js';
  s.onload=function(){window.__mainJsLoaded=true};
  s.onerror=function(){window.__mainJsLoadError=true};
  document.head.appendChild(s);
})();
</script>
```

> 💡 将 `index-xxxx.js` 替换为实际的主 JS 文件名（构建后生成，如 `index-BfmIUzhU.js`）。

### 4.5 验证前端构建产物

```powershell
# 检查 index.html 是否存在
Test-Path "E:\ProgramData\historyApp\world-history-app\front\dist\client\index.html"

# 检查主 JS 文件是否存在
$jsFile = Get-ChildItem "E:\ProgramData\historyApp\world-history-app\front\dist\client\assets\index-*.js" | Select-Object -First 1
Write-Host "主JS文件: $($jsFile.Name), 大小: $([math]::Round($jsFile.Length/1MB, 2))MB"
```

---

## 五、一键启动脚本使用

### 5.1 启动服务

**双击**项目根目录下的 `启动服务.bat`，即可一键启动后端服务。

**预期效果**：
- 弹出 PowerShell 窗口
- 显示「正在启动世界历史学习平台...」
- 显示「服务器运行在 http://localhost:3000」
- 自动打开浏览器访问 http://localhost:3000

### 5.2 停止服务

**双击**项目根目录下的 `停止服务.bat`，即可停止后端服务。

或在 PowerShell 中执行：
```powershell
Get-NetTCPConnection -LocalPort 3000 | ForEach-Object { Stop-Process -Id $_.OwningProcess -Force }
```

### 5.3 bat 脚本常见问题

#### 问题：双击后窗口一闪而过

**原因**：bat 文件编码问题（UTF-8 + LF 换行会导致 cmd 乱码），或脚本中路径错误。

**解决方案**：
1. 右键 bat 文件 → 编辑，检查内容是否有乱码
2. 确保 bat 文件使用 **ANSI 编码**和 **CRLF 换行符**
3. 在 bat 文件末尾添加 `pause`，查看错误信息
4. 或直接使用 PowerShell 运行 `start.ps1`

#### 问题：启动后 3000 端口被占用

**原因**：之前的后端进程未正常关闭。

**解决方案**：
```powershell
# 查看占用 3000 端口的进程
netstat -ano | findstr :3000

# 结束进程（替换 PID）
taskkill /PID <PID> /F

# 或一键清理
Get-NetTCPConnection -LocalPort 3000 -ErrorAction SilentlyContinue | ForEach-Object { Stop-Process -Id $_.OwningProcess -Force -ErrorAction SilentlyContinue }
```

然后重新双击 `启动服务.bat`。

---

## 六、手机端访问配置

### 6.1 前提条件

- 手机和电脑连接**同一个 Wi-Fi/局域网**
- 电脑防火墙允许 3000 端口入站

### 6.2 查看电脑局域网 IP

```powershell
ipconfig | findstr "IPv4"
```

**预期输出**：
```
IPv4 地址 . . . . . . . . . . . . : 192.168.2.5
```

记录这个 IP 地址（如 `192.168.2.5`）。

### 6.3 手机端访问

手机浏览器输入：
```
http://192.168.2.5:3000
```

（将 `192.168.2.5` 替换为你的电脑实际 IP）

### 6.4 手机端白屏排查

如果手机端页面白屏（标题正常但内容空白）：

1. **确认后端 CSP 配置**：`server.js` 中 helmet 必须设置 `upgradeInsecureRequests: null`
2. **确认主 JS 动态加载**：`index.html` 中主 JS 已改为动态加载（无 `crossorigin`）
3. **清除手机浏览器缓存**：设置 → 隐私 → 清除浏览数据
4. **更换浏览器测试**：如 Chrome、Edge、Safari 等
5. **查看手机浏览器控制台**：连接电脑调试，查看 Console 和 Network 错误

> ⚠️ **白屏根本原因**：helmet 默认的 `upgrade-insecure-requests` CSP 指令会强制浏览器将 HTTP 资源升级为 HTTPS，服务器只支持 HTTP 导致主 JS 加载失败。

---

## 七、常见问题实战解决方案

### 7.1 学习进度不同步

**现象**：标记事件已读后，重新登录进度丢失。

**排查步骤**：

1. **确认存储模式**：设置 → 存储与同步 → 确认选择「云端同步」
2. **确认已登录**：设置 → 账号管理 → 确认显示用户信息
3. **打开开发者工具**：F12 → Network 标签
4. **标记事件已读**，查看是否有 `POST /api/progress/update` 请求
5. **如果没有请求** → `isCloudMode()` 返回 false，检查存储模式设置
6. **如果有请求但失败** → 查看状态码（401=token问题，500=后端错误）
7. **手动同步后验证数据库**：
   ```powershell
   # 检查数据库中的进度记录
   $nodePath = "E:\ProgramData\historyApp\node-v22\node-v22.12.0-win-x64"
   $env:PATH = "$nodePath;$env:PATH"
   Set-Location "E:\ProgramData\historyApp\world-history-app\backend"
   node -e "const db=require('./node_modules/better-sqlite3')('./data/history.db',{readonly:true});console.log(db.prepare('SELECT COUNT(*) c FROM learning_progress').get());db.close();"
   ```

**已修复的常见原因**：
- `isCloudMode()` 从 localStorage 读取失败（safeStorage 写入降级到内存）→ 改为从 useAppStore 内存状态读取
- 手动同步只拉取不推送 → 改为先推送再拉取
- loadFromCloud 直接覆盖本地数据 → 改为合并模式（取并集）

### 7.2 学习时长不同步

**现象**：学习时长统计在重新登录后清零。

**已修复的原因**（4处 API 接口不匹配）：
1. `studySessionsApi.create` 中 `start?.id` 应为 `start?.session_id` → end 接口不调用
2. duration 单位不匹配（前端传分钟，后端要秒）
3. `studySessionsApi.list` 返回格式不匹配（后端返回 `{ list }`）
4. loadFromCloud 中字段名错误（`s.duration_minutes` 应为 `s.duration`）

### 7.3 前端构建卡住

**现象**：执行构建命令后长时间无响应。

**解决方案**：
1. 删除 `dist` 目录后重新构建
2. 检查系统资源（任务管理器 → 内存/CPU 使用）
3. 关闭其他占用资源的程序
4. 使用构建日志输出到文件：
   ```powershell
   node node_modules/vite/bin/vite.js build --config vite.config.ts *> build.log
   Get-Content build.log -Tail 20
   ```

### 7.4 登录提示「用户不存在」

**原因**：后端登录接口只接受 `account` 字段，前端发送 `email` 字段。

**解决方案**：后端登录接口已同时支持 `account` 和 `email` 字段。

### 7.5 登录报错「Cannot read properties of undefined (reading 'id')」

**原因**：前端直接用 fetch 返回的 data 访问 `data.user`，实际应该是 `data.data.user`。

**解决方案**：使用统一 API 层 `authApi.login()`，自动处理响应格式。

### 7.6 密码输入框双眼睛图标

**原因**：浏览器密码管理功能自动添加显示按钮，与代码中手动添加的 Eye/EyeOff 按钮重复。

**解决方案**：在 CSS 中添加规则隐藏浏览器自带的密码显示按钮（已修复）。

### 7.7 人物关系图谱移动端白屏

**原因**：
1. 图表容器高度 `calc(100vh - 140px)` 在移动端计算不准确
2. ReactECharts 组件在移动端初始化时机有问题

**解决方案**（已修复）：
1. 图表容器高度改为 `60vh` + `minHeight: 400px`
2. 改用直接初始化 ECharts（useRef + useEffect）
3. 延迟 100ms 初始化图表
4. 监听窗口 resize 事件

---

## 八、验证清单

部署完成后，按以下清单逐项验证：

### 8.1 后端验证

- [ ] 后端服务启动，控制台显示「服务器运行在 http://localhost:3000」
- [ ] 访问 `http://localhost:3000/api/health` 返回 `success: true`
- [ ] 使用管理员账号登录成功，返回 token
- [ ] 数据库文件 `backend/data/history.db` 存在
- [ ] 数据库中有 37 个事件、44 个人物、33 条关系

### 8.2 前端验证

- [ ] 访问 `http://localhost:3000` 页面正常显示
- [ ] 页面标题显示「世界历史学习平台」
- [ ] 底部/顶部导航正常显示
- [ ] 时间线页面正常显示事件节点
- [ ] 人物关系图谱页面正常显示力导向图
- [ ] 点击事件节点可查看详情
- [ ] 四级难度切换正常

### 8.3 功能验证

- [ ] 注册新用户成功
- [ ] 登录/退出登录正常
- [ ] 标记事件已读，进度更新
- [ ] 收藏事件/人物正常
- [ ] 添加笔记（富文本编辑器）正常
- [ ] 完成测验，记录保存
- [ ] 学习时长自动计时
- [ ] 成就徽章解锁
- [ ] 设置中切换存储模式（本地/云端）
- [ ] 手动同步成功
- [ ] 导出学习数据成功
- [ ] 导入学习数据成功
- [ ] 按类型清除数据成功

### 8.4 云端同步验证

- [ ] 登录后标记事件已读
- [ ] 手动同步
- [ ] 退出登录
- [ ] 重新登录
- [ ] 学习进度保留
- [ ] 学习时长保留
- [ ] 收藏保留
- [ ] 笔记保留

### 8.5 移动端验证

- [ ] 手机浏览器访问 `http://<电脑IP>:3000` 正常显示
- [ ] 页面布局适配手机屏幕
- [ ] 底部导航正常
- [ ] 人物关系图谱正常显示（不白屏）
- [ ] 点击人物节点查看详情，弹窗内容不溢出
- [ ] 登录/注册功能正常
- [ ] 学习进度同步正常

---

## 九、日常运维

### 9.1 启动服务

```powershell
# 方法一：双击 启动服务.bat

# 方法二：PowerShell 手动启动
$nodePath = "E:\ProgramData\historyApp\node-v22\node-v22.12.0-win-x64"
$env:PATH = "$nodePath;$env:PATH"
Set-Location "E:\ProgramData\historyApp\world-history-app\backend"
node server.js
```

### 9.2 停止服务

```powershell
# 方法一：双击 停止服务.bat

# 方法二：PowerShell 手动停止
Get-NetTCPConnection -LocalPort 3000 -ErrorAction SilentlyContinue | ForEach-Object { Stop-Process -Id $_.OwningProcess -Force -ErrorAction SilentlyContinue }
```

### 9.3 重启服务

```powershell
# 先停止，再启动
Get-NetTCPConnection -LocalPort 3000 -ErrorAction SilentlyContinue | ForEach-Object { Stop-Process -Id $_.OwningProcess -Force -ErrorAction SilentlyContinue }
Start-Sleep -Seconds 2
$nodePath = "E:\ProgramData\historyApp\node-v22\node-v22.12.0-win-x64"
$env:PATH = "$nodePath;$env:PATH"
Set-Location "E:\ProgramData\historyApp\world-history-app\backend"
Start-Process -FilePath "$nodePath\node.exe" -ArgumentList "server.js" -WorkingDirectory "E:\ProgramData\historyApp\world-history-app\backend" -WindowStyle Hidden
```

### 9.4 查看服务状态

```powershell
# 检查端口是否监听
netstat -ano | findstr :3000

# 健康检查
Invoke-RestMethod -Uri "http://localhost:3000/api/health"

# 查看 node 进程
Get-Process node -ErrorAction SilentlyContinue | Select-Object Id, StartTime, CPU
```

### 9.5 数据库备份

```powershell
# 复制数据库文件
Copy-Item "E:\ProgramData\historyApp\world-history-app\backend\data\history.db" "E:\ProgramData\historyApp\world-history-app\backend\data\history_backup_$(Get-Date -Format 'yyyyMMdd_HHmmss').db"
```

建议定期备份，特别是在数据导入或系统升级前。

### 9.6 查看数据库数据

```powershell
$nodePath = "E:\ProgramData\historyApp\node-v22\node-v22.12.0-win-x64"
$env:PATH = "$nodePath;$env:PATH"
Set-Location "E:\ProgramData\historyApp\world-history-app\backend"

# 查看用户数
node -e "const db=require('./node_modules/better-sqlite3')('./data/history.db',{readonly:true});console.log('用户数:',db.prepare('SELECT COUNT(*) c FROM users').get().c);db.close();"

# 查看事件数
node -e "const db=require('./node_modules/better-sqlite3')('./data/history.db',{readonly:true});console.log('事件数:',db.prepare('SELECT COUNT(*) c FROM events').get().c);db.close();"

# 查看人物数
node -e "const db=require('./node_modules/better-sqlite3')('./data/history.db',{readonly:true});console.log('人物数:',db.prepare('SELECT COUNT(*) c FROM people').get().c);db.close();"

# 查看学习进度数
node -e "const db=require('./node_modules/better-sqlite3')('./data/history.db',{readonly:true});console.log('学习进度:',db.prepare('SELECT COUNT(*) c FROM learning_progress').get().c);db.close();"
```

---

## 附录

### A. 默认账号

| 角色 | 邮箱 | 密码 |
|------|------|------|
| 管理员 | admin@history.com | admin123456 |

> ⚠️ 生产环境请立即修改默认密码！

### B. 常用端口

| 服务 | 默认端口 | 说明 |
|------|----------|------|
| 后端 API + 前端静态 | 3000 | 生产环境 |
| 前端开发服务器 | 5173 | 开发模式 |

### C. 相关文档

- [项目文档.html](./项目文档.html) - 项目完整文档
- [前后端集成与部署指南.md](./前后端集成与部署指南.md) - 通用部署指南
- [API接口文档.md](./API接口文档.md) - 后端 API 接口文档
- [更新日志.md](./更新日志.md) - 版本迭代记录

---

**文档版本**：V3.0  
**更新日期**：2026-09-06  
**适用环境**：Windows 10/11 x64 + Node.js v22.x
