# 世界历史学习平台 - 更新日志

> 本文档记录世界历史学习平台的所有版本迭代、功能新增、Bug 修复和优化改进。
>
> **最新版本**：V3.0 | **更新日期**：2026-09-06

---

## 目录

- [V3.0（2026-09-06）](#v302026-09-06)
- [V2.1（2026-09-05）](#v212026-09-05)
- [V2.0（2026-09-05）](#v202026-09-05)
- [V1.1（2026-09-04）](#v112026-09-04)
- [V1.0（2026-09-04）](#v102026-09-04)

---

## V3.0（2026-09-06）

> **主题**：人物关系网络图谱 + 云端同步全面修复 + 数据管理增强

### 新增功能

#### 1. 人物关系网络图谱
- 新增「人物关系网络图谱」页面（`/person-network`）
- 使用 ECharts 力导向图展示历史人物关系
- 支持 15 种关系类型：君臣、师生、对手、政敌、父子、兄弟、夫妻、祖孙、朋友、盟友、反叛、继承、同学、同时代思想家、其他
- 支持按朝代筛选
- 点击节点查看人物详情和关系列表
- 支持缩放、拖拽、漫游
- 移动端底部导航添加「人物关系」入口

#### 2. 后端人物关系 API
- 新增 `GET /api/people/network/graph` - 获取关系网络图谱数据
- 新增 `POST /api/people/relations` - 添加单条关系
- 新增 `POST /api/people/batch` - 批量导入人物
- 新增 `POST /api/people/relations/batch` - 批量导入关系
- 新增 `person_relations` 数据库表

#### 3. 人物关系数据
- 导入中国古代史 44 位重要人物
- 导入 33 条人物关系
- 覆盖先秦、秦汉、三国、唐宋、明清等主要朝代

#### 4. 数据导入功能
- 设置页面新增「导入学习数据」功能
- 支持 JSON 格式导入学习进度、收藏、笔记、测验记录等
- 导入后自动合并到当前数据

#### 5. 数据清除增强
- 设置页面「清除数据」功能全面升级
- 支持选择清除范围：仅本地 / 仅云端 / 全部清除
- 支持按数据类型选择：学习进度、收藏、笔记、测验记录、错题本、成就徽章、学习时长
- 支持全选/取消全选
- 后端新增 `POST /api/data/clear` 接口

### Bug 修复

#### 1. 学习进度云端同步（核心修复）
- **问题**：标记事件已读 → 手动同步 → 退出登录 → 重新登录后进度清零
- **原因1**：退出登录时 `setStorageMode('local')` 重置了存储模式
  - **修复**：退出登录不再改变存储模式
- **原因2**：`isCloudMode()` 每次从 localStorage 读取，但 safeStorage 写入可能失败降级到内存，导致 localStorage 中是旧值
  - **修复**：改为从 `useAppStore.getState().storageMode` 内存状态读取
- **原因3**：手动同步只调用 loadFromCloud（从云端拉取），没有 pushToCloud（把本地推送到云端）
  - **修复**：添加 pushToCloud() 和 syncAll() 方法，手动同步改为先推送再拉取
- **原因4**：loadFromCloud 直接覆盖本地数据，云端空数据会清空本地进度
  - **修复**：改为合并模式（数组取并集、按ID合并、统计取最大值）

#### 2. 学习时长同步（4处 API 接口不匹配）
- **问题**：事件同步没问题，但学习时长统计没有同步
- **Bug1**：`studySessionsApi.create` 中 `start?.id` 应为 `start?.session_id`
  - 影响：后端 start 接口返回字段名是 session_id，导致 end 接口永远不调用，数据没保存
- **Bug2**：`studySessionsApi.create` 中传 `duration_minutes`（分钟），后端 end 接口要 `duration`（秒）
  - 影响：字段名和单位都不匹配
- **Bug3**：`studySessionsApi.list` 后端返回 `{ list: [...], pagination: {...} }`，前端直接 `request<any[]>()` 当成数组处理
  - 影响：拉取时空数组，云端时长读不回来
- **Bug4**：loadFromCloud 中学习时长转换读 `s.duration_minutes`，后端字段是 `s.duration`（秒）
  - 影响：时长计算为0，显示的学习时长不对

#### 3. 移动端人物关系图谱白屏
- **问题**：人物关系图谱页面在移动端空白，桌面端正常
- **原因1**：图表容器高度 `calc(100vh - 140px)` 在移动端计算不准确
  - **修复**：改为 `60vh` + `minHeight: 400px`
- **原因2**：ReactECharts 组件在移动端初始化时机有问题
  - **修复**：改用直接初始化 ECharts（useRef + useEffect），不依赖 ReactECharts 组件
  - 延迟 100ms 初始化图表，确保 DOM 已渲染
  - 监听窗口 resize 事件，自动调用 chart.resize()

#### 4. 人物详情弹窗内容溢出
- **问题**：手机端人物详情弹窗中，关系描述文字被截断超出弹窗范围
- **修复**：
  - 设置弹窗最大高度 `max-height: 80vh`
  - 内容区域添加 `overflow-y: auto`
  - 文字添加 `word-break: break-word` 和 `white-space: pre-wrap`
  - 关系列表每项添加底部边框和间距

### 优化改进

- API 接口全面检查：系统检查 13 个路由模块、55 个接口，确认除学习时长外其余接口前后端均匹配
- 统一 API 服务层：`utils/api.ts` 封装所有后端接口，request 函数自动返回 `data.data ?? data`
- 构建优化：构建卡住时先删除 dist 目录再重试，用 `*> build.log` 重定向输出
- 文档完善：更新项目文档、部署指南、API 接口文档、更新日志

### 数据变更

- 数据库表：新增 `person_relations` 表（16张表）
- 历史人物：44位（中国古代史）
- 人物关系：33条
- 后端路由：13个（新增 data.js）
- 前端页面：18个（新增 PersonNetworkPage）

---

## V2.1（2026-09-05）

> **主题**：登录与设置页面 Bug 修复 + 移动端适配

### Bug 修复

#### 1. 登录接口字段不匹配
- **问题**：登录管理员账号提示「用户不存在」
- **原因**：后端登录接口只接受 `account` 字段，前端发送 `email` 字段
- **修复**：后端登录接口同时支持 `account` 和 `email` 字段

#### 2. 登录响应数据结构不匹配
- **问题**：登录成功但前端报错「Cannot read properties of undefined (reading 'id')」
- **原因**：前端直接用 fetch 返回的 data 访问 `data.user`，实际应该是 `data.data.user`
- **修复**：改用统一 API 层 `authApi.login()`，自动处理响应格式

#### 3. 设置页面登录状态显示错误
- **问题**：已登录但设置页面显示「需要登录」
- **原因**：设置页面使用本地 useState 管理登录状态，未从全局 store 获取
- **修复**：改为从 `useAppStore` 获取 `currentUser`

#### 4. 密码输入框双眼睛图标
- **问题**：密码输入框右侧出现两个眼睛图标
- **原因**：浏览器密码管理功能自动添加显示按钮，与代码中手动添加的 Eye/EyeOff 按钮重复
- **修复**：在 CSS 中添加规则隐藏浏览器自带的密码显示按钮

#### 5. 时间线圆钮无法交互
- **问题**：第一次世界大战棕色圆钮右边的蓝色圆钮无法点击，也没有信息
- **原因**：TimelineTrack 组件中部分事件节点缺少点击事件处理
- **修复**：为所有事件节点添加点击事件和信息展示

#### 6. 右下角平台水印悬浮组件
- **问题**：页面右下角显示平台水印悬浮组件，影响用户体验
- **修复**：通过 CSS 隐藏平台水印悬浮组件

#### 7. "我的"页面头像点击无反应
- **问题**：点击左上角头像，页面没有反应，正常应该有登录/注册入口
- **修复**：ProfileHeader 组件添加头像点击事件，跳转登录/注册页面

### 优化改进

- 移动端底部导航优化：确保所有入口在移动端可访问
- 全局样式优化：添加平台水印隐藏、密码按钮隐藏等 CSS 规则

---

## V2.0（2026-09-05）

> **主题**：真实后端云端同步 + 学习时长计时 + 富文本编辑器升级 + 内容扩充

### 新增功能

#### 1. 真实后端云端同步
- 后端技术栈：Node.js + Express + better-sqlite3 + JWT + bcryptjs + helmet
- 11 个路由模块：auth、events、progress、notes、favorites、quizzes、achievements、studySessions、stats、sync、health
- 14 张数据表：users、learning_progress、notes、favorites、quiz_records、wrong_questions、achievements、study_sessions、daily_stats、events、people、quizzes、sync_log、badge_definitions、refresh_tokens
- JWT 认证机制，Token 有效期 7 天
- 后端托管前端静态文件 + SPA 回退，前后端共用 3000 端口

#### 2. 双模式存储
- 本地保存模式：数据保存在浏览器 localStorage
- 云端同步模式：数据同步到服务器，支持多设备访问
- 用户可在设置中自主选择存储模式
- 手动同步按钮：推送本地到云端 + 从云端拉取合并

#### 3. 学习时长自动计时
- 自动记录用户学习时长
- 开始/结束学习会话机制
- 学习时长统计展示
- 学习日历热力图

#### 4. 富文本编辑器升级
- 替换原编辑器为 Tiptap 3
- 支持文字、列表、链接、加粗、斜体等格式
- 笔记编辑体验更完善

#### 5. 内容扩充
- 历史事件从 30 个扩充至 37 个
- 覆盖中国古代史核心事件（先秦至明清）
- 创建数据导入工具：importAllEvents.js、generateFrontendEvents.js

### Bug 修复

#### 1. 手机端白屏（关键修复）
- **问题**：手机浏览器访问页面，标题正常但内容空白
- **根本原因**：helmet 默认添加 `upgrade-insecure-requests` CSP 指令，强制浏览器将 HTTP 资源请求升级为 HTTPS。服务器只支持 HTTP，导致主 JS 加载失败
- **修复**：
  - 后端 helmet 配置显式设置 `upgradeInsecureRequests: null`
  - 主 JS 改为动态加载（移除 `crossorigin` 属性）
  - 添加全局错误显示 div，方便排查移动端问题

#### 2. 前端构建命令 Windows 不兼容
- **问题**：`npm run build:client` 在 Windows PowerShell 报错
- **原因**：package.json 中的构建脚本使用 Unix 语法（`NODE_ENV=production vite build`）
- **修复**：使用手动命令构建：`$env:NODE_ENV="production"; node node_modules/vite/bin/vite.js build --config vite.config.ts`

#### 3. better-sqlite3 版本不兼容
- **问题**：better-sqlite3@9.x 在 Node v22+ Windows 无预编译二进制，安装失败
- **修复**：升级到 better-sqlite3@11.x

### 优化改进

- Node.js 版本要求：前端 >=22，后端 >=16
- 便携版 Node.js 方案：不修改系统环境变量，临时设置 PATH
- 前端依赖安装使用 `--ignore-scripts`，避免 postinstall 脚本失败
- index.html 模板变量替换：11 个平台注入变量需手动替换
- 一键启动脚本：启动服务.bat / 停止服务.bat

---

## V1.1（2026-09-04）

> **主题**：基础功能完善 + 四级难度体系优化

### 新增功能

- 四级难度内容切换：幼儿园/小学/初中/高中
- 学习进度追踪：已读事件/人物标记
- 成就系统：多种成就徽章
- 收藏功能：事件/人物收藏
- 搜索功能：全局搜索历史事件和人物
- 学习笔记：基础笔记功能

### Bug 修复

- 时间线事件节点显示优化
- 移动端响应式布局调整
- 事件详情页内容展示优化

### 优化改进

- 首页学习概览页面优化
- 导航栏布局调整
- 全局样式统一

---

## V1.0（2026-09-04）

> **主题**：世界历史学习平台初始版本

### 初始功能

#### 1. 交互式时间线
- 按时间顺序展示所有历史事件
- 支持缩放、拖拽
- 事件节点颜色区分时代
- 点击节点查看详情

#### 2. 事件百科
- 30 个历史事件
- 每个事件包含背景、过程、影响
- 四级难度分级内容

#### 3. 人物百科
- 历史人物列表和详情
- 按朝代筛选

#### 4. 历史地图
- 按时代切换地图
- 疆域/政权标注

#### 5. 互动测验
- 四级难度题库
- 选择题型
- 即时评分和解析

#### 6. 学习笔记
- 基础笔记功能
- 按事件/人物关联

#### 7. 学习进度
- 已读事件/人物统计
- 时代完成度进度条

#### 8. 成就系统
- 多种成就徽章
- 解锁条件追踪

#### 9. 四级难度分级
- 幼儿园：趣味故事、简单概念
- 小学：基础知识、趣味互动
- 初中：系统知识、时间线、事件因果
- 高中：深度分析、人物关系、专题研究

#### 10. 响应式设计
- 适配手机端、平板端、电脑端
- 移动端底部导航
- 桌面端顶部导航

### 技术栈

- 前端：React 19 + TypeScript + Vite 7 + Tailwind CSS 4 + Radix UI + Zustand + React Router 6
- 状态管理：Zustand（useAppStore、useLearningStore）
- 数据存储：浏览器 localStorage（纯前端模式）
- 内置数据：30 个历史事件、内置题库

---

## 版本统计

| 版本 | 日期 | 新增功能 | 修复 Bug | 数据扩充 |
|------|------|----------|----------|----------|
| V1.0 | 2026-09-04 | 10大基础功能 | - | 30事件 |
| V1.1 | 2026-09-04 | 进度/成就/收藏/搜索 | 3+ | - |
| V2.0 | 2026-09-05 | 真实后端/双模式/学习时长/富文本 | 5+ | 37事件 |
| V2.1 | 2026-09-05 | - | 7 | - |
| V3.0 | 2026-09-06 | 人物关系图谱/数据导入/清除增强 | 8+ | 44人物+33关系 |

### 当前数据规模（V3.0）

| 数据类型 | 数量 |
|----------|------|
| 历史事件 | 37个 |
| 历史人物 | 44位 |
| 人物关系 | 33条 |
| 数据库表 | 16张 |
| 后端路由 | 13个 |
| API 接口 | 55个 |
| 前端页面 | 18个 |

---

## 后续规划

### 数据扩充
- [ ] 中国近代史事件和人物
- [ ] 中国现代史事件和人物
- [ ] 世界古代史事件和人物
- [ ] 世界近代史事件和人物
- [ ] 世界现代史事件和人物
- [ ] 人物关系数据扩充（世界史部分）

### 功能优化
- [ ] 错题本云端同步
- [ ] 成就主动推送（后端 unlock 接口）
- [ ] 学习时长会话优化（减少创建频率）
- [ ] 历史地图详细化（按现在的地图详细制作）
- [ ] 更多互动测验题目
- [ ] 学习数据可视化图表

### 性能优化
- [ ] 前端代码分割（动态 import）
- [ ] 图片压缩和懒加载
- [ ] 数据库索引优化
- [ ] 静态资源缓存配置

---

**文档版本**：V3.0  
**更新日期**：2026-09-06  
**首次发布**：2026-09-04
