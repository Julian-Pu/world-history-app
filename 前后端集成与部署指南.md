# 世界历史学习平台 - 前后端集成与部署指南

> 本文档详细介绍世界历史学习平台的前后端部署、集成、配置和常见问题解决方案。适用于开发人员和运维人员。

---

## 目录

1. [项目概述](#一项目概述)
2. [环境要求](#二环境要求)
3. [后端部署](#三后端部署)
4. [前端部署](#四前端部署)
5. [前后端集成](#五前后端集成)
6. [数据导入](#六数据导入)
7. [双模式存储说明](#七双模式存储说明)
8. [常见问题排查](#八常见问题排查)
9. [生产环境建议](#九生产环境建议)

---

## 一、项目概述

本项目是一个世界历史学习平台，采用前后端分离架构：

- **前端应用**：React 19 + TypeScript + Vite 7 + Tailwind CSS 4，响应式设计，适配手机/平板/电脑
- **后端服务**：Node.js + Express + better-sqlite3，提供 RESTful API 和静态文件托管
- **数据库**：SQLite（单文件数据库，无需额外安装数据库服务）

### 核心功能

- 交互式时间线、事件/人物百科、历史地图
- 人物关系网络图谱（ECharts 力导向图）
- 互动测验、学习笔记（Tiptap 3 富文本）、收藏
- 学习进度追踪、学习时长自动计时、成就系统
- 四级难度分级（幼儿园/小学/初中/高中）
- 双模式存储（本地保存/云端同步，用户可自主选择）
- 数据导入/导出、按类型清除数据

### 项目结构

```
world-history-app/
├── 项目文档.html                    # 项目完整文档
├── 前后端集成与部署指南.md           # 本文档
├── 部署与运行实战指南.md             # 实战部署指南（Windows）
├── API接口文档.md                    # 后端 API 接口文档
├── 更新日志.md                       # 版本迭代记录
├── 启动服务.bat                      # 一键启动脚本（Windows）
├── 停止服务.bat                      # 停止服务脚本（Windows）
├── start.ps1                         # PowerShell 启动逻辑
├── backend/                          # 后端项目
│   ├── server.js                     # 服务入口（含前端静态托管+SPA回退）
│   ├── package.json                  # 依赖配置
│   ├── config/database.js            # 数据库配置与建表（16张表）
│   ├── middleware/auth.js            # JWT 认证中间件
│   ├── routes/                       # 13个路由模块
│   ├── utils/                        # 工具脚本（初始化/导入/数据生成）
│   ├── data/                         # 历史数据 JSON + SQLite 数据库
│   │   ├── china/                    # 中国史事件数据
│   │   ├── world/                    # 世界史事件数据
│   │   ├── people/                   # 人物关系数据
│   │   └── history.db                # SQLite 数据库文件（运行后生成）
│   └── README.md                     # 后端说明
└── front/                            # 前端项目（monorepo）
    ├── client/                       # 前端源码
    │   └── src/
    │       ├── pages/                # 18个页面组件
    │       ├── components/           # 通用组件
    │       ├── data/                 # 内置历史数据
    │       ├── store/                # Zustand 状态管理
    │       ├── utils/                # 工具函数（API层、安全存储）
    │       └── index.css             # 全局样式
    ├── dist/client/                  # 前端构建产物（构建后生成）
    ├── package.json                  # 依赖配置（要求 Node >=22）
    └── vite.config.ts                # Vite 构建配置
```

---

## 二、环境要求

### 2.1 硬件要求

| 项目 | 最低要求 | 推荐配置 |
|------|----------|----------|
| 操作系统 | Windows 10 / macOS 10.15 / Ubuntu 20.04 | Windows 10/11 x64 |
| 内存 | 4GB | 8GB（前端构建需约 2GB） |
| 磁盘空间 | 2GB | 5GB（node_modules 约 1.5GB） |
| CPU | 双核 | 四核及以上 |

### 2.2 软件要求

| 软件 | 版本要求 | 说明 |
|------|----------|------|
| Node.js | **>= 22.0.0** | 前端构建必须，后端建议同版本 |
| npm | >= 10.0.0 | 随 Node.js 安装 |
| 浏览器 | Chrome 90+ / Edge 90+ / Safari 14+ / Firefox 88+ | 推荐使用现代浏览器 |

### 2.3 便携版 Node.js（推荐 Windows 用户）

如果系统 Node.js 版本低于 22，建议使用便携版，不修改系统环境变量：

1. 下载 Node.js v22.x Windows 二进制包（`node-v22.x.x-win-x64.zip`）
2. 解压到项目同级目录，如 `E:\ProgramData\historyApp\node-v22\`
3. 使用时在命令行临时设置 PATH：
   ```powershell
   $nodePath = "E:\ProgramData\historyApp\node-v22\node-v22.12.0-win-x64"
   $env:PATH = "$nodePath;$env:PATH"
   node --version  # 应显示 v22.x.x
   ```

> ⚠️ **注意**：不要将便携版 Node 路径添加到系统 PATH，避免与系统 Node 冲突。每次使用前临时设置即可。

---

## 三、后端部署

### 3.1 安装依赖

```bash
cd backend
npm install
```

主要依赖：
- `express`：Web 框架
- `better-sqlite3`：SQLite 数据库驱动（同步 API，性能好）
- `jsonwebtoken`：JWT 认证
- `bcryptjs`：密码加密
- `helmet`：安全头设置
- `cors`：跨域支持
- `express-rate-limit`：限流

> 💡 **better-sqlite3 版本**：必须使用 ^11.7.0 及以上版本，旧版本（如 9.x）在 Node v22+ Windows 无预编译二进制，会导致安装失败。

### 3.2 环境变量配置

复制 `.env.example` 为 `.env`（如果存在），或直接使用默认配置：

```env
PORT=3000                    # 服务端口
NODE_ENV=development         # 运行环境（development/production）
JWT_SECRET=your-secret-key   # JWT密钥（生产环境务必修改为随机字符串）
JWT_EXPIRES_IN=7d            # JWT过期时间
DB_PATH=./data/history.db    # 数据库文件路径
```

> ⚠️ **生产环境务必修改 JWT_SECRET**，使用随机字符串，避免安全风险。

### 3.3 初始化数据库

首次运行前需要初始化数据库，创建数据表和默认管理员：

```bash
node utils/initDatabase.js
```

该命令会自动：
1. 创建 `data/history.db` 数据库文件
2. 创建 16 张数据表（用户、进度、笔记、收藏、测验、成就、学习时长、人物关系等）
3. 创建默认管理员账号：
   - 邮箱：`admin@history.com`
   - 密码：`admin123456`

> ⚠️ **生产环境请立即修改默认管理员密码！**

### 3.4 导入历史数据

```bash
# 导入历史事件数据
node utils/importAllEvents.js

# 导入人物和关系数据
node utils/importPeopleRelations.js
```

数据文件位于 `backend/data/` 目录：
- `china/`：中国史事件 JSON 文件
- `world/`：世界史事件 JSON 文件
- `people/`：人物关系 JSON 文件

### 3.5 启动服务

```bash
node server.js
```

服务启动后，控制台会显示：
```
服务器运行在 http://localhost:3000
数据库连接成功
```

### 3.6 验证服务

访问健康检查接口：
```
http://localhost:3000/api/health
```

正常返回：
```json
{
  "success": true,
  "message": "API is running",
  "timestamp": "2026-09-06T..."
}
```

### 3.7 后端可用脚本

| 命令 | 说明 |
|------|------|
| `node server.js` | 启动后端服务 |
| `node utils/initDatabase.js` | 初始化数据库（创建表+默认管理员） |
| `node utils/importAllEvents.js` | 批量导入历史事件数据 |
| `node utils/importPeopleRelations.js` | 导入人物和关系数据 |
| `node utils/generateFrontendEvents.js` | 从数据库生成前端 events.ts |

---

## 四、前端部署

### 4.1 安装依赖

```bash
cd front
npm install --ignore-scripts
```

> 💡 **必须使用 `--ignore-scripts`**：部分依赖（如 better-sqlite3）的 postinstall 脚本在 Windows 可能失败，使用此参数跳过脚本执行，不影响前端功能。

### 4.2 开发模式运行

```bash
npm run dev
```

开发服务器默认运行在 `http://localhost:5173`，支持热更新。

> ⚠️ 开发模式下前端请求后端 API 需要配置代理，或直接使用后端服务的 3000 端口访问（后端已托管前端静态文件）。

### 4.3 生产环境构建

#### Windows 正确构建方式（重要！）

```powershell
cd front
$env:NODE_ENV="production"
node node_modules/vite/bin/vite.js build --config vite.config.ts
```

> ⚠️ **不能使用 `npm run build:client`**：package.json 中的构建脚本使用 Unix 语法（`NODE_ENV=production vite build`），在 Windows PowerShell 中会报错。必须使用上述命令手动设置环境变量后构建。

#### macOS / Linux 构建方式

```bash
cd front
npm run build:client
```

### 4.4 构建产物

构建完成后，产物位于 `front/dist/client/` 目录：
```
dist/client/
├── index.html              # 入口 HTML（含平台模板变量，需替换）
├── assets/                 # JS/CSS/图片等静态资源
│   ├── index-xxxx.js      # 主 JS 文件
│   ├── index-xxxx.css     # 主 CSS 文件
│   └── ...
└── ...
```

### 4.5 替换 index.html 模板变量（重要！）

构建后的 `index.html` 包含 11 个平台注入模板变量，必须手动替换为实际值，否则页面无法正常渲染。

需要替换的变量：

| 变量 | 替换值 | 说明 |
|------|--------|------|
| `{{csrfToken}}` | `""` | CSRF Token（本地部署为空） |
| `{{userId}}` | `""` | 用户ID（未登录为空） |
| `{{tenantId}}` | `""` | 租户ID |
| `{{appId}}` | `"app_17df6r2kvgn"` | 应用ID |
| `{{environment}}` | `"production"` | 运行环境 |
| `{{appName}}` | `"世界历史学习平台"` | 应用名称 |
| `{{{appAvatar}}}` | `""` | 应用头像（三花括号） |
| `{{appDescription}}` | `"世界历史学习平台"` | 应用描述 |
| `{{basename}}` | `"/"` | 基础路径 |
| `{{{__platform__}}}` | `"web"` | 平台类型（三花括号） |
| `{{currentUrl}}` | `"http://localhost:3000"` | 当前URL |

同时需要替换：
- `<title>{{appName}}</title>` → `<title>世界历史学习平台</title>`
- `<meta name="..." content="{{appName}}">` 等 meta 标签

> 💡 **三花括号变量**：`{{{appAvatar}}}` 和 `{{{__platform__}}}` 使用三花括号，表示不转义的原始值，替换时注意格式。

### 4.6 主 JS 动态加载（移动端白屏修复）

为解决移动端 CSP 导致的白屏问题，需要将主 JS 改为动态加载，移除 `crossorigin` 属性：

将：
```html
<script type="module" crossorigin src="/assets/index-xxxx.js"></script>
```

替换为：
```html
<script>
(function(){
  var s=document.createElement('script');
  s.type='module';
  s.src='/assets/index-xxxx.js';
  s.onload=function(){window.__mainJsLoaded=true};
  s.onerror=function(){window.__mainJsLoadError=true};
  document.head.appendChild(s);
})();
</script>
```

### 4.7 全局错误显示（可选但推荐）

在 `<div id="root"></div>` 前添加错误显示 div 和全局错误监听脚本，方便排查移动端白屏问题：

```html
<div id="error-display" style="position:fixed;top:0;left:0;right:0;bottom:0;background:#1a1a2e;color:#fff;padding:20px;z-index:99999;overflow:auto;display:none;font-family:monospace;font-size:14px;line-height:1.6;"></div>
<div id="root"></div>
<script>
window.addEventListener("error",function(e){
  var d=document.getElementById("error-display");
  if(d&&e.error){
    d.style.display="block";
    d.innerHTML="<pre>"+(e.message||"")+"\n"+(e.error.stack||"")+"</pre>";
  }
});
</script>
```

---

## 五、前后端集成

### 5.1 后端托管前端静态文件

后端 `server.js` 已配置前端静态文件托管和 SPA 回退：

```javascript
// 托管前端静态文件
app.use(express.static(path.join(__dirname, '../front/dist/client')));

// SPA 回退：所有非 API 请求返回 index.html
app.get('*', (req, res, next) => {
  if (req.path.startsWith('/api/')) return next();
  res.sendFile(path.join(__dirname, '../front/dist/client/index.html'));
});
```

这样前后端可以共用一个端口（3000），无需额外配置反向代理。

### 5.2 CSP 安全配置（移动端白屏关键）

后端使用 `helmet` 设置安全头，必须注意以下配置，否则会导致移动端白屏：

```javascript
app.use(helmet({
  contentSecurityPolicy: {
    directives: {
      defaultSrc: ["'self'"],
      scriptSrc: ["'self'", "'unsafe-inline'"],
      styleSrc: ["'self'", "'unsafe-inline'"],
      imgSrc: ["'self'", "data:", "https:"],
      connectSrc: ["'self'", "https:"],
      // 关键：禁用 upgrade-insecure-requests，否则 HTTP 环境下移动端会白屏
      upgradeInsecureRequests: null,
    },
  },
  // 禁用以下安全头，避免本地部署和局域网访问问题
  strictTransportSecurity: false,
  crossOriginResourcePolicy: false,
  crossOriginOpenerPolicy: false,
}));
```

> ⚠️ **移动端白屏根本原因**：helmet 默认添加 `upgrade-insecure-requests` CSP 指令，强制浏览器将 HTTP 资源请求升级为 HTTPS。服务器只支持 HTTP，导致主 JS 加载失败，页面白屏。必须显式设置 `upgradeInsecureRequests: null`。

### 5.3 CORS 配置

如果前端和后端部署在不同域名/端口，需要配置 CORS：

```javascript
const cors = require('cors');
app.use(cors({
  origin: true,  // 允许所有来源，生产环境建议指定具体域名
  credentials: true,
}));
```

本项目前后端共用 3000 端口，无需额外配置 CORS。

### 5.4 API 统一响应格式

后端所有 API 统一返回格式：

```json
{
  "success": true,
  "message": "操作成功",
  "data": { ... },
  "timestamp": "2026-09-06T..."
}
```

前端 `utils/api.ts` 的 `request` 函数自动返回 `data.data ?? data`，简化调用。

### 5.5 认证机制

- 登录成功后，后端返回 JWT token
- 前端将 token 保存在 `localStorage`（key: `history-app-token`）
- 后续请求在 `Authorization` 头中携带 `Bearer <token>`
- 后端 `authenticate` 中间件验证 token 有效性

---

## 六、数据导入

### 6.1 历史事件数据导入

1. 准备 JSON 数据文件，格式参考 `backend/data/china/` 目录下的现有文件
2. 将文件放入对应目录（`china/` 或 `world/`）
3. 运行导入脚本：
   ```bash
   cd backend
   node utils/importAllEvents.js
   ```
4. 生成前端 events.ts：
   ```bash
   node utils/generateFrontendEvents.js
   ```
5. 将生成的 `events.ts` 复制到 `front/client/src/data/` 目录
6. 重新构建前端

### 6.2 人物和关系数据导入

1. 准备 JSON 数据文件，格式参考 `backend/data/people/people-china-ancient.json`
2. 运行导入脚本：
   ```bash
   cd backend
   node utils/importPeopleRelations.js
   ```
3. 人物关系数据会自动导入到 `people` 和 `person_relations` 表

### 6.3 数据文件格式示例

**历史事件 JSON 格式**：
```json
[
  {
    "id": "uuid-or-custom-id",
    "title": "事件标题",
    "era": "ancient",
    "region": "asia",
    "start_year": -1046,
    "end_year": -771,
    "location": "地点",
    "key_figure_ids": [],
    "content": {
      "kindergarten": { "summary": "...", "background": "...", "process": "...", "impact": "..." },
      "elementary": { "summary": "...", "background": "...", "process": "...", "impact": "..." },
      "junior": { "summary": "...", "background": "...", "process": "...", "impact": "..." },
      "senior": { "summary": "...", "background": "...", "process": "...", "impact": "..." }
    }
  }
]
```

**人物关系 JSON 格式**：
```json
{
  "people": [
    {
      "id": "uuid-or-custom-id",
      "name": "人物姓名",
      "dynasty": "朝代",
      "region": "地区",
      "role": "身份",
      "birth_year": "生卒年",
      "death_year": "",
      "level_contents": {
        "kindergarten": { "summary": "..." },
        "elementary": { "summary": "..." },
        "junior": { "summary": "..." },
        "senior": { "summary": "..." }
      }
    }
  ],
  "relations": [
    {
      "person_id": "人物ID1",
      "related_person_id": "人物ID2",
      "relation_type": "君臣",
      "description": "关系描述",
      "start_year": "",
      "end_year": ""
    }
  ]
}
```

---

## 七、双模式存储说明

### 7.1 存储模式

用户可在「设置」页面选择存储模式：

| 模式 | 说明 | 适用场景 |
|------|------|----------|
| 本地保存 | 数据仅保存在浏览器 localStorage，不上传服务器 | 单设备使用、注重隐私、无网络环境 |
| 云端同步 | 数据自动同步到服务器，支持多设备访问 | 多设备使用、需要数据备份、跨设备同步 |

### 7.2 自动同步机制

在云端模式下，以下操作会**自动实时同步**到云端：
- 标记事件/人物已读 → `POST /api/progress/update`
- 收藏/取消收藏 → `POST /api/favorites` 或 `DELETE /api/favorites/by-content/...`
- 添加/修改/删除笔记 → `POST/PUT/DELETE /api/notes/...`
- 完成测验 → `POST /api/quizzes/submit`
- 学习时长累计 → `POST /api/study-sessions/start` + `POST /api/study-sessions/:id/end`

### 7.3 手动同步

设置页面提供「手动同步」按钮，执行完整同步流程：
1. **推送**：将本地所有数据推送到云端（进度、收藏、笔记等）
2. **拉取**：从云端拉取所有数据，与本地数据合并（取并集）

### 7.4 数据合并策略

登录后从云端拉取数据时，采用合并策略，避免云端空数据清空本地进度：

| 数据类型 | 合并策略 |
|----------|----------|
| 已读事件/人物 | 取并集（本地 ∪ 云端） |
| 收藏 | 取并集 |
| 笔记 | 按 ID 合并，云端优先 |
| 测验记录 | 按 ID 合并，云端优先 |
| 成就 | 取并集 |
| 学习时长 | 取最大值 |
| 学习日历 | 按日期合并，时长取最大值 |

### 7.5 数据导入/导出

- **导出**：设置页面支持导出学习数据为 JSON 文件
- **导入**：设置页面支持导入 JSON 格式的学习数据，导入后会合并到当前数据

### 7.6 数据清除

设置页面支持按类型清除数据：
- **清除范围**：仅本地 / 仅云端 / 全部清除
- **数据类型**：学习进度、收藏、笔记、测验记录、错题本、成就徽章、学习时长（支持全选）

---

## 八、常见问题排查

### 8.1 移动端白屏

**现象**：手机浏览器访问页面，标题正常但内容空白。

**根本原因**：helmet 默认添加 `upgrade-insecure-requests` CSP 指令，强制浏览器将 HTTP 资源请求升级为 HTTPS。服务器只支持 HTTP，导致主 JS 加载失败。

**解决方案**：
1. 后端 `server.js` 中 helmet 配置显式设置 `upgradeInsecureRequests: null`
2. 主 JS 改为动态加载（移除 `crossorigin` 属性）
3. 清除浏览器缓存后重新访问

**验证方法**：手机浏览器打开开发者工具，查看 Console 是否有「Mixed Content」或「Failed to load resource」错误，查看 Network 标签主 JS 请求状态。

### 8.2 端口被占用

**现象**：启动后端时报错 `Port 3000 is already in use`。

**解决方案**：
```powershell
# 查看占用 3000 端口的进程
netstat -ano | findstr :3000

# 结束进程（替换 PID 为实际进程号）
taskkill /PID <PID> /F
```

或修改后端端口：设置环境变量 `PORT=3001`，然后启动。

### 8.3 前端构建失败

**现象**：执行构建命令后报错或卡住。

**常见原因及解决方案**：

1. **Node.js 版本过低**
   - 检查：`node --version`
   - 要求：>= 22.0.0
   - 解决：使用便携版 Node v22，临时设置 PATH

2. **使用了错误的构建命令**
   - Windows 不能使用 `npm run build:client`（Unix 语法）
   - 必须使用：`$env:NODE_ENV="production"; node node_modules/vite/bin/vite.js build --config vite.config.ts`

3. **依赖安装失败**
   - 使用 `npm install --ignore-scripts` 安装
   - 删除 `node_modules` 和 `package-lock.json` 后重新安装

4. **构建卡住**
   - 删除 `dist` 目录后重新构建
   - 检查系统资源（内存/CPU）是否充足

### 8.4 bat 脚本一闪而过

**现象**：双击 `启动服务.bat` 后，窗口一闪而过，服务未启动。

**原因**：bat 文件编码问题（UTF-8 + LF 换行会导致 cmd 乱码），或脚本中路径错误。

**解决方案**：
1. 使用 ANSI 编码保存 bat 文件
2. 使用 CRLF 换行符
3. 在 bat 文件末尾添加 `pause` 查看错误信息
4. 或直接使用 PowerShell 运行 `start.ps1`

### 8.5 学习进度不同步

**现象**：标记事件已读后，重新登录进度丢失。

**排查步骤**：
1. 确认设置中存储模式为「云端同步」
2. 确认已登录（有 token）
3. 打开浏览器开发者工具 → Network 标签
4. 标记事件已读，查看是否有 `POST /api/progress/update` 请求
5. 如果没有请求 → `isCloudMode()` 返回 false，检查存储模式设置
6. 如果有请求但失败 → 查看请求状态码和响应内容
7. 手动同步后，检查数据库 `learning_progress` 表是否有数据

**常见原因**：
- `isCloudMode()` 从 localStorage 读取失败（safeStorage 写入降级到内存）
- 解决方案：`isCloudMode()` 改为从 useAppStore 内存状态读取

### 8.6 学习时长不同步

**现象**：学习时长统计在重新登录后清零。

**常见原因**（已修复）：
1. `studySessionsApi.create` 中 `start?.id` 应为 `start?.session_id` → 导致 end 接口不调用
2. duration 单位不匹配（前端传分钟，后端要秒）
3. `studySessionsApi.list` 返回格式不匹配（后端返回 `{ list }`，前端直接当数组）
4. loadFromCloud 中字段名错误（`s.duration_minutes` 应为 `s.duration`）

### 8.7 登录提示「用户不存在」

**现象**：输入正确的管理员账号，提示用户不存在。

**原因**：后端登录接口只接受 `account` 字段，前端发送 `email` 字段。

**解决方案**：后端登录接口同时支持 `account` 和 `email` 字段（已修复）。

### 8.8 登录报错「Cannot read properties of undefined (reading 'id')」

**现象**：登录成功但前端报错。

**原因**：前端直接用 fetch 返回的 data 访问 `data.user`，实际应该是 `data.data.user`。

**解决方案**：使用统一 API 层 `authApi.login()`，自动处理响应格式（已修复）。

### 8.9 密码输入框双眼睛图标

**现象**：密码输入框右侧出现两个眼睛图标。

**原因**：浏览器密码管理功能自动添加显示按钮，与代码中手动添加的 Eye/EyeOff 按钮重复。

**解决方案**：在 CSS 中添加规则隐藏浏览器自带的密码显示按钮（已修复）。

### 8.10 人物关系图谱移动端白屏

**现象**：人物关系图谱页面在移动端空白，桌面端正常。

**原因**：
1. 图表容器高度 `calc(100vh - 140px)` 在移动端计算不准确
2. ReactECharts 组件在移动端初始化时机有问题

**解决方案**：
1. 图表容器高度改为 `60vh` + `minHeight: 400px`
2. 改用直接初始化 ECharts（useRef + useEffect），不依赖 ReactECharts 组件
3. 延迟 100ms 初始化图表，确保 DOM 已渲染
4. 监听窗口 resize 事件，自动调用 chart.resize()

---

## 九、生产环境建议

### 9.1 安全配置

1. **修改默认管理员密码**：首次部署后立即修改 `admin@history.com` 的密码
2. **修改 JWT_SECRET**：使用随机字符串，避免使用默认值
3. **配置 HTTPS**：生产环境建议使用 HTTPS，可通过 Nginx 反向代理实现
4. **限制 CORS 来源**：生产环境指定具体域名，不使用 `origin: true`
5. **配置 rate-limit**：限制 API 请求频率，防止暴力破解

### 9.2 性能优化

1. **前端代码分割**：使用动态 import 分割大组件，减少首屏加载时间
2. **图片压缩**：优化图片大小，使用 WebP 格式
3. **数据库索引**：为常用查询字段添加索引（user_id, content_type, content_id 等）
4. **静态资源缓存**：配置 Nginx 缓存静态资源（JS/CSS/图片）
5. **Gzip 压缩**：启用 Gzip 压缩，减少传输体积

### 9.3 数据备份

1. **定期备份数据库文件**：`backend/data/history.db`
2. **备份用户上传数据**：如果有用户上传的图片/文件
3. **备份配置文件**：`.env`、`server.js` 等配置文件

### 9.4 监控和日志

1. **应用日志**：记录 API 请求、错误、异常
2. **性能监控**：监控响应时间、错误率、资源使用
3. **用户行为分析**：统计活跃用户、学习时长、功能使用情况

### 9.5 部署架构建议

```
用户 → Nginx（HTTPS + 静态资源缓存 + Gzip）→ Node.js 后端（Express + SQLite）
                                    ↓
                              前端静态文件（dist/client）
```

- Nginx 负责 HTTPS 终止、静态资源缓存、Gzip 压缩
- Node.js 后端负责 API 服务和动态内容
- SQLite 数据库文件定期备份

---

## 附录

### A. 默认账号

| 角色 | 邮箱 | 密码 |
|------|------|------|
| 管理员 | admin@history.com | admin123456 |

> ⚠️ 生产环境请立即修改默认密码！

### B. 常用端口

| 服务 | 默认端口 | 说明 |
|------|----------|------|
| 后端 API + 前端静态 | 3000 | 生产环境 |
| 前端开发服务器 | 5173 | 开发模式（npm run dev） |

### C. 相关文档

- [项目文档.html](./项目文档.html) - 项目完整文档（功能、架构、数据模型）
- [部署与运行实战指南.md](./部署与运行实战指南.md) - Windows 环境实战部署指南
- [API接口文档.md](./API接口文档.md) - 后端 API 接口详细说明
- [更新日志.md](./更新日志.md) - 版本迭代记录

---

**文档版本**：V3.0  
**更新日期**：2026-09-06  
**适用版本**：世界历史学习平台 V3.0
